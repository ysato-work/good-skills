#!/usr/bin/env bash
# PreToolUse hook — セルフレビューゲートの装填と deny 判定

set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
PLUGIN_ROOT="$(cd "${SCRIPT_DIR}/.." && pwd)"

exec node "${PLUGIN_ROOT}/lib/gate-hook.mjs" arm
